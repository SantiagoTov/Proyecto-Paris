# Proyecto París

This is a React project initialized with Vite and TypeScript.

## 🚨 IMPORTANT RULE: Material UI Only 🚨

**ALL frontend components and styles MUST use [Material UI (MUI)](https://mui.com/).**

- **Do NOT** use raw CSS or other styling libraries unless absolutely necessary.
- **Do NOT** use Tailwind CSS.
- **Do NOT** use inline styles heavily; use the `sx` prop or `styled()` from MUI.
- All new components must import from `@mui/material` or `@mui/icons-material`.

## Getting Started

1.  Install dependencies:
    ```bash
    npm install
    ```
2.  Start the development server:
    ```bash
    npm run dev
    ```

## Project Structure

- `src/`: Source code
- `public/`: Static assets
